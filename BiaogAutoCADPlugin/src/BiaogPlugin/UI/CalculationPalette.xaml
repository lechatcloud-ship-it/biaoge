<UserControl x:Class="BiaogPlugin.UI.CalculationPalette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Width="400" Background="#1E1E1E">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="15">
            <TextBlock Text="表哥 - 构件识别算量"
                       FontSize="20" FontWeight="Bold"
                       Foreground="White" Margin="0,0,0,20"/>

            <Border Background="#2D2D30" CornerRadius="8" Padding="15">
                <StackPanel>
                    <TextBlock Text="功能开发中..." FontSize="14"
                               Foreground="#CCCCCC" TextAlignment="Center"/>
                    <TextBlock Text="即将推出超高精度构件识别功能" FontSize="12"
                               Foreground="#999999" TextAlignment="Center" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
