<Window x:Class="BiaogPlugin.UI.KeybindingsManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="快捷键管理"
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Topmost="True"
        Background="#2D2D30"
        Foreground="#FFFFFF"
        WindowStyle="None"
        AllowsTransparency="True"
        BorderBrush="#007ACC"
        BorderThickness="1"
        ResizeMode="CanResize">

    <Window.Resources>
        <!-- 按钮样式 -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3F3F46"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#007ACC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- DataGrid样式 -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#252526"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#3F3F46"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#3F3F46"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CanUserResizeRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="RowHeight" Value="32"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#3F3F46"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderBrush" Value="#007ACC"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
        </Style>

        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="#252526"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3F3F46"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#007ACC"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridCell">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Foreground" Value="#FFFFFF"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- 标题栏 -->
        <Border Grid.Row="0" Background="#3F3F46" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid>
                <TextBlock Text="快捷键管理"
                           FontSize="16"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="15,0,0,0"
                           Foreground="#FFFFFF"/>
                <Button Content="✕"
                        Width="40"
                        Height="40"
                        HorizontalAlignment="Right"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="#FFFFFF"
                        FontSize="18"
                        Click="CloseButton_Click"
                        Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#E81123"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>

        <!-- 快捷键列表 -->
        <GroupBox Grid.Row="1" Header="已配置的快捷键"
                  Margin="10"
                  Padding="10"
                  Foreground="#FFFFFF"
                  BorderBrush="#3F3F46">
            <DataGrid x:Name="KeybindingsGrid">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="快捷键"
                                       Binding="{Binding Shortcut}"
                                       Width="100"
                                       FontWeight="Bold"
                                       Foreground="#4EC9B0"/>
                    <DataGridTextColumn Header="命令名称"
                                       Binding="{Binding CommandName}"
                                       Width="250"/>
                    <DataGridTextColumn Header="功能说明"
                                       Binding="{Binding Description}"
                                       Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- 说明信息 -->
        <Border Grid.Row="2"
                Background="#1E1E1E"
                Padding="15"
                Margin="10,0,10,10"
                BorderBrush="#3F3F46"
                BorderThickness="1"
                CornerRadius="4">
            <StackPanel>
                <TextBlock Text="💡 使用说明："
                           FontSize="14"
                           FontWeight="Bold"
                           Foreground="#4EC9B0"
                           Margin="0,0,0,8"/>
                <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#CCCCCC">
                    <Run Text="• 点击"/>
                    <Run Text="「导出配置」" FontWeight="Bold" Foreground="#FFFFFF"/>
                    <Run Text="可将快捷键配置保存到桌面（.pgp文件）"/>
                </TextBlock>
                <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#CCCCCC" Margin="0,4,0,0">
                    <Run Text="• 点击"/>
                    <Run Text="「自动安装」" FontWeight="Bold" Foreground="#FFFFFF"/>
                    <Run Text="可自动添加快捷键到AutoCAD配置文件（会自动备份）"/>
                </TextBlock>
                <TextBlock TextWrapping="Wrap" FontSize="12" Foreground="#CCCCCC" Margin="0,4,0,0">
                    <Run Text="• 安装后需要在AutoCAD中运行"/>
                    <Run Text="REINIT" FontWeight="Bold" Foreground="#FFFFFF"/>
                    <Run Text="命令重新加载PGP文件"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- 按钮区域 -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10,0,10">
            <Button Content="📄 导出配置"
                    Style="{StaticResource ModernButton}"
                    Click="ExportButton_Click"
                    Margin="0,0,10,0"
                    Width="130"/>
            <Button Content="⚙️ 自动安装"
                    Style="{StaticResource ModernButton}"
                    Click="InstallButton_Click"
                    Margin="0,0,10,0"
                    Width="130"/>
            <Button Content="❓ 查看指南"
                    Style="{StaticResource ModernButton}"
                    Click="GuideButton_Click"
                    Margin="0,0,10,0"
                    Width="130"/>
            <Button Content="关闭"
                    Style="{StaticResource ModernButton}"
                    Click="CloseButton_Click"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>
