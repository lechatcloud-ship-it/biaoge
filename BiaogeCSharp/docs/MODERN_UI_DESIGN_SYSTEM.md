# 表哥 C# - UI设计规范

## 设计理念

**基于Avalonia UI原生设计，适度现代化增强**
- 以Avalonia.Themes.Fluent为基础
- 保持原生设计语言的一致性
- 适度增强视觉效果和交互体验
- 简洁、流畅、易用

---

## 🎨 视觉设计

### 色彩系统

#### 主题色（深色主题）

```css
/* 背景层次 */
--bg-primary: #0D0D0D;          /* 主背景 - 更深更纯粹 */
--bg-secondary: #1A1A1A;        /* 二级背景 */
--bg-tertiary: #242424;         /* 三级背景 */
--bg-elevated: rgba(30, 30, 30, 0.95);  /* 浮动层（毛玻璃） */

/* 表面颜色 */
--surface-default: #1E1E1E;     /* 默认表面 */
--surface-hover: #2A2A2A;       /* 悬停状态 */
--surface-pressed: #323232;     /* 按压状态 */
--surface-selected: rgba(0, 120, 212, 0.15);  /* 选中状态 */

/* 强调色 */
--accent-primary: #0078D4;      /* 主色 */
--accent-primary-hover: #1A86E0;
--accent-primary-pressed: #005A9E;
--accent-gradient: linear-gradient(135deg, #0078D4 0%, #00BCF2 100%);

/* 语义色 */
--success: #10B981;
--success-bg: rgba(16, 185, 129, 0.1);
--warning: #F59E0B;
--warning-bg: rgba(245, 158, 11, 0.1);
--error: #EF4444;
--error-bg: rgba(239, 68, 68, 0.1);
--info: #3B82F6;
--info-bg: rgba(59, 130, 246, 0.1);

/* 文本颜色 */
--text-primary: rgba(255, 255, 255, 0.95);
--text-secondary: rgba(255, 255, 255, 0.7);
--text-tertiary: rgba(255, 255, 255, 0.5);
--text-disabled: rgba(255, 255, 255, 0.3);

/* 边框 */
--border-default: rgba(255, 255, 255, 0.08);
--border-hover: rgba(255, 255, 255, 0.12);
--border-focus: #0078D4;
```

#### 毛玻璃效果（Acrylic）

```css
/* 标准毛玻璃 */
background: rgba(30, 30, 30, 0.7);
backdrop-filter: blur(40px) saturate(180%);
-webkit-backdrop-filter: blur(40px) saturate(180%);

/* 强毛玻璃（更清晰） */
background: rgba(30, 30, 30, 0.85);
backdrop-filter: blur(60px) saturate(200%);

/* 轻毛玻璃（更透明） */
background: rgba(30, 30, 30, 0.5);
backdrop-filter: blur(30px) saturate(150%);
```

### 阴影系统

```css
/* 高度层次 */
--shadow-xs: 0 1px 2px 0 rgba(0, 0, 0, 0.3);
--shadow-sm: 0 2px 4px 0 rgba(0, 0, 0, 0.4);
--shadow-md: 0 4px 8px 0 rgba(0, 0, 0, 0.5);
--shadow-lg: 0 8px 16px 0 rgba(0, 0, 0, 0.6);
--shadow-xl: 0 16px 32px 0 rgba(0, 0, 0, 0.7);
--shadow-2xl: 0 24px 48px 0 rgba(0, 0, 0, 0.8);

/* 彩色阴影（强调元素） */
--shadow-accent: 0 8px 24px 0 rgba(0, 120, 212, 0.4);
--shadow-success: 0 8px 24px 0 rgba(16, 185, 129, 0.3);
--shadow-error: 0 8px 24px 0 rgba(239, 68, 68, 0.3);
```

### 圆角系统

```css
--radius-xs: 4px;   /* 小元素（按钮、输入框） */
--radius-sm: 6px;   /* 小卡片 */
--radius-md: 8px;   /* 标准卡片 */
--radius-lg: 12px;  /* 大卡片 */
--radius-xl: 16px;  /* 面板 */
--radius-2xl: 20px; /* 模态框 */
--radius-full: 9999px;  /* 圆形 */
```

### 字体系统

```css
/* 字体家族 */
--font-sans: 'Microsoft YaHei UI', 'Segoe UI', system-ui, sans-serif;
--font-mono: 'Cascadia Code', 'Consolas', monospace;

/* 字号 */
--text-xs: 11px;
--text-sm: 12px;
--text-base: 14px;
--text-lg: 16px;
--text-xl: 18px;
--text-2xl: 24px;
--text-3xl: 30px;
--text-4xl: 36px;

/* 行高 */
--leading-tight: 1.25;
--leading-normal: 1.5;
--leading-relaxed: 1.75;

/* 字重 */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

---

## ⚡ 动画系统

### 过渡时间

```css
--duration-instant: 0ms;
--duration-fast: 100ms;
--duration-normal: 200ms;
--duration-slow: 300ms;
--duration-slower: 500ms;
```

### 缓动函数

```css
--ease-default: cubic-bezier(0.4, 0.0, 0.2, 1);
--ease-in: cubic-bezier(0.4, 0.0, 1, 1);
--ease-out: cubic-bezier(0.0, 0.0, 0.2, 1);
--ease-in-out: cubic-bezier(0.4, 0.0, 0.6, 1);
--ease-spring: cubic-bezier(0.68, -0.55, 0.265, 1.55);
```

### 常用动画

#### 淡入淡出
```css
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes fadeOut {
    from { opacity: 1; }
    to { opacity: 0; }
}
```

#### 滑动
```css
@keyframes slideUp {
    from { transform: translateY(20px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
}

@keyframes slideDown {
    from { transform: translateY(-20px); opacity: 0; }
    to { transform: translateY(0); opacity: 1; }
}

@keyframes slideRight {
    from { transform: translateX(-20px); opacity: 0; }
    to { transform: translateX(0); opacity: 1; }
}
```

#### 缩放
```css
@keyframes scaleIn {
    from { transform: scale(0.9); opacity: 0; }
    to { transform: scale(1); opacity: 1; }
}

@keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}
```

#### 加载动画
```css
@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

@keyframes shimmer {
    0% { background-position: -1000px 0; }
    100% { background-position: 1000px 0; }
}
```

---

## 🎯 组件设计规范

### 按钮

#### 主要按钮（Primary）
```xaml
<Button
    Background="#0078D4"
    Foreground="White"
    Padding="16,10"
    CornerRadius="6"
    FontSize="14"
    FontWeight="Medium"
    BorderThickness="0">
    <Button.Effect>
        <DropShadowEffect Color="#0078D4" Opacity="0.4" BlurRadius="12" />
    </Button.Effect>
    <Button.Styles>
        <Style Selector="Button:pointerover">
            <Setter Property="Background" Value="#1A86E0"/>
            <Setter Property="RenderTransform" Value="scale(1.02)"/>
        </Style>
        <Style Selector="Button:pressed">
            <Setter Property="Background" Value="#005A9E"/>
            <Setter Property="RenderTransform" Value="scale(0.98)"/>
        </Style>
    </Button.Styles>
</Button>
```

#### 次要按钮（Secondary）
- 透明背景，边框强调
- Hover时背景淡入

#### 幽灵按钮（Ghost）
- 完全透明
- Hover时轻微背景

### 卡片（现代化）

```xaml
<Border
    Background="rgba(30, 30, 30, 0.7)"
    CornerRadius="12"
    Padding="24"
    Margin="12"
    BorderBrush="rgba(255, 255, 255, 0.08)"
    BorderThickness="1">

    <Border.Effect>
        <DropShadowEffect
            Color="#000000"
            Opacity="0.5"
            BlurRadius="16"
            OffsetY="4"/>
    </Border.Effect>

    <!-- 毛玻璃效果需要自定义控件支持 -->
</Border>
```

### 输入框

```xaml
<TextBox
    Background="#1E1E1E"
    Foreground="White"
    BorderBrush="rgba(255, 255, 255, 0.08)"
    BorderThickness="1"
    CornerRadius="6"
    Padding="12,8"
    FontSize="14">
    <TextBox.Styles>
        <Style Selector="TextBox:focus">
            <Setter Property="BorderBrush" Value="#0078D4"/>
            <Setter Property="Effect">
                <DropShadowEffect Color="#0078D4" Opacity="0.3" BlurRadius="8"/>
            </Setter>
        </Style>
        <Style Selector="TextBox:pointerover">
            <Setter Property="BorderBrush" Value="rgba(255, 255, 255, 0.12)"/>
        </Style>
    </TextBox.Styles>
</TextBox>
```

### Toast通知

```
┌─────────────────────────────────┐
│  ✓  操作成功                    │
│     DWG文件已加载               │
│                            [×]  │
└─────────────────────────────────┘
```

特性：
- 从右上角滑入
- 3秒后自动消失
- 支持图标和操作按钮
- 可堆叠多个通知

---

## 🖱️ 交互设计

### 微交互反馈

#### Hover状态
- 背景色变化
- 轻微放大（scale: 1.02）
- 阴影加深
- 过渡时间: 100ms

#### Click状态
- 轻微缩小（scale: 0.98）
- 阴影收缩
- 过渡时间: 50ms

#### Focus状态
- 蓝色边框
- 外发光效果
- 不影响布局

### 页面过渡

#### 导航切换
1. 当前页面淡出（200ms）
2. 新页面从右侧滑入（300ms，延迟50ms）
3. 缓动函数：ease-out

#### 模态框
1. 背景遮罩淡入（200ms）
2. 对话框缩放+淡入（300ms）
3. 缓动函数：ease-spring

### 加载状态

#### Skeleton屏幕
```
┌────────────────────────┐
│ ▓▓▓▓▓▓▓▓              │  ← 标题
│ ▓▓▓▓▓▓▓▓▓▓▓▓          │  ← 描述
│ ▓▓▓▓  ▓▓▓▓  ▓▓▓▓      │  ← 按钮组
└────────────────────────┘
```

特性：
- 灰色占位块
- 流动的光泽动画（shimmer）
- 真实内容的形状预览

#### 进度环
- 圆形进度条
- 中心显示百分比
- 颜色渐变

---

## 📐 布局规范

### 间距系统

```css
--space-xs: 4px;
--space-sm: 8px;
--space-md: 12px;
--space-lg: 16px;
--space-xl: 24px;
--space-2xl: 32px;
--space-3xl: 48px;
--space-4xl: 64px;
```

### 容器宽度

```css
--container-xs: 480px;
--container-sm: 640px;
--container-md: 768px;
--container-lg: 1024px;
--container-xl: 1280px;
--container-2xl: 1536px;
```

### 响应式断点

```css
--breakpoint-sm: 640px;
--breakpoint-md: 768px;
--breakpoint-lg: 1024px;
--breakpoint-xl: 1280px;
--breakpoint-2xl: 1536px;
```

---

## 🎪 特殊效果

### 拖放区域

```
┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
│                           │
│    📂 拖放DWG文件到此     │
│    或点击选择文件         │
│                           │
└ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
```

悬停时：
- 虚线边框变实线
- 背景色高亮
- 图标放大

### 空状态

```
       📭
   暂无数据
 快来上传第一个DWG文件吧
   [  打开文件  ]
```

特性：
- 大图标
- 友好的文案
- 主要操作按钮

### 错误状态

```
       ⚠️
   加载失败
 文件可能已损坏或格式不支持
   [  重新尝试  ]  [  选择其他文件  ]
```

---

## 🔤 图标系统

使用 **Fluent System Icons** 或 **Segoe MDL2 Assets**

常用图标映射：
- Home: 🏠 →
- Translation: 🌐 →
- Calculation: 📊 →
- Export: 📤 →
- Settings: ⚙ →
- Folder: 📁 →
- File: 📄 →
- Success: ✓ →
- Error: ✕ →
- Warning: ⚠ →

---

## 📱 首次使用体验

### 欢迎向导（3步）

**第1步：欢迎**
```
┌──────────────────────────────────┐
│                                  │
│         欢迎使用表哥             │
│                                  │
│   专业的建筑工程CAD翻译工具      │
│                                  │
│            [  开始  ]            │
│                                  │
└──────────────────────────────────┘
```

**第2步：配置API**
```
┌──────────────────────────────────┐
│  配置阿里云百炼API               │
│                                  │
│  API密钥: [_______________]      │
│                                  │
│  ☑ 记住我的选择                  │
│                                  │
│  [  跳过  ]        [  下一步  ]  │
└──────────────────────────────────┘
```

**第3步：完成**
```
┌──────────────────────────────────┐
│         🎉 一切就绪！            │
│                                  │
│  现在可以开始翻译DWG文件了       │
│                                  │
│            [  开始使用  ]        │
│                                  │
└──────────────────────────────────┘
```

---

## 🎨 示例：现代化主窗口

```
┌────────────────────────────────────────────────────────────┐
│  📐 表哥                                    🔍  ⚙  👤  ×  │ ← 毛玻璃标题栏
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────┐                                                 │
│  │ 🏠   │  主页                                           │
│  ├──────┤                                                 │
│  │ 🌐   │  翻译                                           │
│  ├──────┤                                                 │
│  │ 📊   │  算量      ┌─────────────────────────────┐     │
│  ├──────┤            │                             │     │
│  │ 📤   │  导出      │     DWG查看器区域          │     │
│  └──────┘            │                             │     │
│    200px             │   (带缩放、拖动等工具)     │     │
│                      │                             │     │
│                      └─────────────────────────────┘     │
│                                                            │
├────────────────────────────────────────────────────────────┤
│  就绪 | 实体: 0 | 图层: 0            ━━━━━━━━━━  60 FPS │ ← 状态栏
└────────────────────────────────────────────────────────────┘
```

---

## 🚀 实施优先级

### P0（立即实施）
1. ✅ 更新色彩系统到2.0
2. ✅ 实现按钮的现代化样式
3. ✅ 添加基础动画（hover、click）
4. ✅ 优化卡片阴影和圆角

### P1（本周）
1. 实现Toast通知系统
2. 添加拖放文件功能
3. 实现首次使用向导
4. 优化空状态和错误状态

### P2（下周）
1. 实现Acrylic毛玻璃效果
2. 添加Skeleton加载动画
3. 优化页面切换动画
4. 完善图标系统

---

**版本**: v2.0
**最后更新**: 2025-01-XX
