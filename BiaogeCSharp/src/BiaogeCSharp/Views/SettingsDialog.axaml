<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d" d:DesignWidth="700" d:DesignHeight="600"
        x:Class="BiaogeCSharp.Views.SettingsDialog"
        Title="设置"
        Width="700" Height="600"
        Background="#1E1E1E"
        WindowStartupLocation="CenterOwner"
        CanResize="False">

    <DockPanel>
        <!-- 底部按钮栏 -->
        <Border DockPanel.Dock="Bottom" Background="#2D2D30" Padding="20,15" BorderBrush="#3E3E42" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10">
                <Button Name="ApplyButton"
                        Content="应用"
                        FontSize="14"
                        Height="36"
                        Padding="20,0"
                        Background="#0078D4"
                        Foreground="White"
                        BorderThickness="0"
                        CornerRadius="4">
                    <Button.Styles>
                        <Style Selector="Button:pointerover">
                            <Setter Property="Background" Value="#1F8FE8"/>
                        </Style>
                    </Button.Styles>
                </Button>

                <Button Name="OkButton"
                        Content="确定"
                        FontSize="14"
                        Height="36"
                        Padding="20,0"
                        Background="#0078D4"
                        Foreground="White"
                        BorderThickness="0"
                        CornerRadius="4">
                    <Button.Styles>
                        <Style Selector="Button:pointerover">
                            <Setter Property="Background" Value="#1F8FE8"/>
                        </Style>
                    </Button.Styles>
                </Button>

                <Button Name="CancelButton"
                        Content="取消"
                        FontSize="14"
                        Height="36"
                        Padding="20,0"
                        Background="#3E3E42"
                        Foreground="White"
                        BorderThickness="0"
                        CornerRadius="4">
                    <Button.Styles>
                        <Style Selector="Button:pointerover">
                            <Setter Property="Background" Value="#505050"/>
                        </Style>
                    </Button.Styles>
                </Button>
            </StackPanel>
        </Border>

        <!-- 主内容：选项卡 -->
        <TabControl Background="#1E1E1E" Margin="0">
            <!-- Tab 1: 阿里云百炼 -->
            <TabItem Header="阿里云百炼">
                <ScrollViewer>
                    <StackPanel Margin="20" Spacing="20">
                        <!-- API密钥 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="API密钥" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBox Grid.Column="0" Name="ApiKeyTextBox" Watermark="sk-xxxxxxxxxxxxxxxx" PasswordChar="●" FontSize="13"/>
                                <Button Grid.Column="1" Content="👁" Margin="5,0,0,0" Width="36" Height="36" Padding="0"/>
                            </Grid>
                            <TextBlock Text="从阿里云百炼控制台获取API密钥" Foreground="#CCCCCC" FontSize="12"/>
                        </StackPanel>

                        <!-- API端点 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="API端点" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <TextBox Text="https://dashscope.aliyuncs.com/api/v1" FontSize="13"/>
                        </StackPanel>

                        <!-- 超时设置 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="请求超时 (秒)" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <NumericUpDown Value="30" Minimum="5" Maximum="300" Increment="5" FormatString="0"/>
                        </StackPanel>

                        <!-- 重试次数 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="失败重试次数" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <NumericUpDown Value="3" Minimum="0" Maximum="10" Increment="1" FormatString="0"/>
                        </StackPanel>

                        <!-- 模型选择 -->
                        <StackPanel Spacing="8">
                            <TextBlock Text="多模态对话模型" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="0" FontSize="13">
                                <ComboBoxItem Content="qwen-vl-max (推荐)"/>
                                <ComboBoxItem Content="qwen-vl-plus"/>
                                <ComboBoxItem Content="qwen-max"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="图片翻译模型" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="0" FontSize="13">
                                <ComboBoxItem Content="qwen-vl-max (推荐)"/>
                                <ComboBoxItem Content="qwen-vl-plus"/>
                                <ComboBoxItem Content="qwen-mt-image"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="文本翻译模型" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="0" FontSize="13">
                                <ComboBoxItem Content="qwen-mt-plus (推荐)"/>
                                <ComboBoxItem Content="qwen-mt-turbo"/>
                                <ComboBoxItem Content="qwen-plus"/>
                                <ComboBoxItem Content="qwen-turbo"/>
                                <ComboBoxItem Content="qwen-max"/>
                            </ComboBox>
                        </StackPanel>

                        <!-- 自定义模型 -->
                        <StackPanel Spacing="8">
                            <CheckBox Content="使用自定义模型" Foreground="White"/>
                            <TextBox Watermark="输入自定义模型名称" FontSize="13"/>
                        </StackPanel>

                        <!-- 测试连接 -->
                        <Button Content="测试连接"
                                FontSize="14"
                                Height="36"
                                Padding="20,0"
                                Background="#0078D4"
                                Foreground="White"
                                BorderThickness="0"
                                CornerRadius="4"
                                HorizontalAlignment="Left">
                            <Button.Styles>
                                <Style Selector="Button:pointerover">
                                    <Setter Property="Background" Value="#1F8FE8"/>
                                </Style>
                            </Button.Styles>
                        </Button>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 2: 翻译设置 -->
            <TabItem Header="翻译设置">
                <ScrollViewer>
                    <StackPanel Margin="20" Spacing="20">
                        <StackPanel Spacing="8">
                            <TextBlock Text="批处理大小" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <NumericUpDown Value="50" Minimum="1" Maximum="200" Increment="10" FormatString="0"/>
                            <TextBlock Text="每批次翻译的文本数量" Foreground="#CCCCCC" FontSize="12"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="并发线程数" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <NumericUpDown Value="3" Minimum="1" Maximum="10" Increment="1" FormatString="0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="启用缓存" Foreground="White" IsChecked="True"/>
                            <TextBlock Text="缓存已翻译的文本，提高效率并降低成本" Foreground="#CCCCCC" FontSize="12" Margin="25,0,0,0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="启用质量控制" Foreground="White" IsChecked="True"/>
                            <TextBlock Text="自动验证翻译质量，确保99.9999%准确率" Foreground="#CCCCCC" FontSize="12" Margin="25,0,0,0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="保留原文格式" Foreground="White" IsChecked="True"/>
                            <TextBlock Text="保留尺寸、单位等特殊格式" Foreground="#CCCCCC" FontSize="12" Margin="25,0,0,0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="默认源语言" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="0" FontSize="13">
                                <ComboBoxItem Content="中文 (简体)"/>
                                <ComboBoxItem Content="中文 (繁体)"/>
                                <ComboBoxItem Content="自动检测"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="默认目标语言" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="0" FontSize="13">
                                <ComboBoxItem Content="英语"/>
                                <ComboBoxItem Content="日语"/>
                                <ComboBoxItem Content="韩语"/>
                                <ComboBoxItem Content="法语"/>
                                <ComboBoxItem Content="德语"/>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 3: 性能优化 -->
            <TabItem Header="性能优化">
                <ScrollViewer>
                    <StackPanel Margin="20" Spacing="20">
                        <StackPanel Spacing="8">
                            <CheckBox Content="启用空间索引" Foreground="White" IsChecked="True"/>
                            <TextBlock Text="使用R-tree空间索引加速实体查询（推荐）" Foreground="#CCCCCC" FontSize="12" Margin="25,0,0,0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="启用抗锯齿" Foreground="White" IsChecked="True"/>
                            <TextBlock Text="提高渲染质量，但会增加CPU使用率" Foreground="#CCCCCC" FontSize="12" Margin="25,0,0,0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="实体加载阈值" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <NumericUpDown Value="100000" Minimum="10000" Maximum="1000000" Increment="10000" FormatString="N0"/>
                            <TextBlock Text="超过此数量的实体将延迟加载" Foreground="#CCCCCC" FontSize="12"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="渲染帧率限制 (FPS)" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <NumericUpDown Value="60" Minimum="30" Maximum="144" Increment="15" FormatString="0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="内存使用阈值 (MB)" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <NumericUpDown Value="500" Minimum="200" Maximum="4000" Increment="100" FormatString="N0"/>
                            <TextBlock Text="超过此值将触发内存清理" Foreground="#CCCCCC" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 4: 界面设置 -->
            <TabItem Header="界面设置">
                <ScrollViewer>
                    <StackPanel Margin="20" Spacing="20">
                        <StackPanel Spacing="8">
                            <TextBlock Text="主题" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="0" FontSize="13">
                                <ComboBoxItem Content="深色 (推荐)"/>
                                <ComboBoxItem Content="浅色"/>
                                <ComboBoxItem Content="跟随系统"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="语言" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="0" FontSize="13">
                                <ComboBoxItem Content="简体中文"/>
                                <ComboBoxItem Content="English"/>
                                <ComboBoxItem Content="日本語"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="字体大小" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="1" FontSize="13">
                                <ComboBoxItem Content="小 (12px)"/>
                                <ComboBoxItem Content="中 (14px)"/>
                                <ComboBoxItem Content="大 (16px)"/>
                                <ComboBoxItem Content="特大 (18px)"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="启用动画效果" Foreground="White" IsChecked="True"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="显示工具提示" Foreground="White" IsChecked="True"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="启动时显示欢迎页面" Foreground="White" IsChecked="True"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 5: 数据管理 -->
            <TabItem Header="数据管理">
                <ScrollViewer>
                    <StackPanel Margin="20" Spacing="20">
                        <StackPanel Spacing="8">
                            <TextBlock Text="配置文件位置" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBox Grid.Column="0" Text="~/.biaoge/config.json" IsReadOnly="True" FontSize="13"/>
                                <Button Grid.Column="1" Content="打开" Margin="5,0,0,0" Padding="15,0"/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="缓存目录" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBox Grid.Column="0" Text="~/.biaoge/cache/" IsReadOnly="True" FontSize="13"/>
                                <Button Grid.Column="1" Content="打开" Margin="5,0,0,0" Padding="15,0"/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="日志目录" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <Grid ColumnDefinitions="*,Auto">
                                <TextBox Grid.Column="0" Text="~/.biaoge/logs/" IsReadOnly="True" FontSize="13"/>
                                <Button Grid.Column="1" Content="打开" Margin="5,0,0,0" Padding="15,0"/>
                            </Grid>
                        </StackPanel>

                        <Separator Background="#3E3E42" Margin="0,10"/>

                        <StackPanel Spacing="10">
                            <Button Content="导出配置"
                                    FontSize="14"
                                    Height="36"
                                    Padding="20,0"
                                    Background="#3E3E42"
                                    Foreground="White"
                                    BorderThickness="0"
                                    CornerRadius="4"
                                    HorizontalAlignment="Left">
                                <Button.Styles>
                                    <Style Selector="Button:pointerover">
                                        <Setter Property="Background" Value="#505050"/>
                                    </Style>
                                </Button.Styles>
                            </Button>

                            <Button Content="导入配置"
                                    FontSize="14"
                                    Height="36"
                                    Padding="20,0"
                                    Background="#3E3E42"
                                    Foreground="White"
                                    BorderThickness="0"
                                    CornerRadius="4"
                                    HorizontalAlignment="Left">
                                <Button.Styles>
                                    <Style Selector="Button:pointerover">
                                        <Setter Property="Background" Value="#505050"/>
                                    </Style>
                                </Button.Styles>
                            </Button>

                            <Button Content="清理缓存"
                                    FontSize="14"
                                    Height="36"
                                    Padding="20,0"
                                    Background="#9D5D00"
                                    Foreground="White"
                                    BorderThickness="0"
                                    CornerRadius="4"
                                    HorizontalAlignment="Left">
                                <Button.Styles>
                                    <Style Selector="Button:pointerover">
                                        <Setter Property="Background" Value="#B86E00"/>
                                    </Style>
                                </Button.Styles>
                            </Button>

                            <Button Content="清理日志"
                                    FontSize="14"
                                    Height="36"
                                    Padding="20,0"
                                    Background="#9D5D00"
                                    Foreground="White"
                                    BorderThickness="0"
                                    CornerRadius="4"
                                    HorizontalAlignment="Left">
                                <Button.Styles>
                                    <Style Selector="Button:pointerover">
                                        <Setter Property="Background" Value="#B86E00"/>
                                    </Style>
                                </Button.Styles>
                            </Button>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <!-- Tab 6: 高级 -->
            <TabItem Header="高级">
                <ScrollViewer>
                    <StackPanel Margin="20" Spacing="20">
                        <StackPanel Spacing="8">
                            <CheckBox Content="启用调试模式" Foreground="White"/>
                            <TextBlock Text="显示详细的调试信息（影响性能）" Foreground="#CCCCCC" FontSize="12" Margin="25,0,0,0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <TextBlock Text="日志级别" Foreground="White" FontSize="14" FontWeight="SemiBold"/>
                            <ComboBox SelectedIndex="1" FontSize="13">
                                <ComboBoxItem Content="DEBUG"/>
                                <ComboBoxItem Content="INFO"/>
                                <ComboBoxItem Content="WARNING"/>
                                <ComboBoxItem Content="ERROR"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="发送匿名使用统计" Foreground="White"/>
                            <TextBlock Text="帮助我们改进产品（不包含任何个人数据）" Foreground="#CCCCCC" FontSize="12" Margin="25,0,0,0"/>
                        </StackPanel>

                        <StackPanel Spacing="8">
                            <CheckBox Content="自动检查更新" Foreground="White" IsChecked="True"/>
                        </StackPanel>

                        <Separator Background="#3E3E42" Margin="0,10"/>

                        <StackPanel Spacing="10">
                            <Button Content="重置所有设置"
                                    FontSize="14"
                                    Height="36"
                                    Padding="20,0"
                                    Background="#C42B1C"
                                    Foreground="White"
                                    BorderThickness="0"
                                    CornerRadius="4"
                                    HorizontalAlignment="Left">
                                <Button.Styles>
                                    <Style Selector="Button:pointerover">
                                        <Setter Property="Background" Value="#E81123"/>
                                    </Style>
                                </Button.Styles>
                            </Button>

                            <TextBlock Text="此操作将恢复所有设置到默认值，且无法撤销！"
                                      Foreground="#C42B1C"
                                      FontSize="12"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
